query users{
  users{
  fullname,
  	comments{
      text,
      post{
        id
        title
      }
    }
    posts{title}
  }
}

query getAllUsers{
  users{
  fullname
  }
}

query getAllComments{
  comments{
  text
  }
}

query getAllPosts{
  posts{
  title
  }
}

mutation addUser{
  createUser(fullname:"Stephen"){
    id,
    fullname,
    comments{text}
    posts{title}
    
  }
}

mutation addComment{
  	createComment(text:"Sample comment 2", post_id:"1",user_id:"1"){
    id
      text,
      post_id
      user_id
    }
}

mutation addPost{
  createPost(title:"Sample Post for Buket", user_id:"1"){
    id,
    title,
    user_id
  }
}